{
  "name": "tilebuilder",
  "version": "1.0.0",
  "description": "Create Mapbox vector tiles from Shape data using TileBuilder",
  "scripts": {
    "build":   "docker-compose run --rm tilebuilder",
    "start":   "docker-compose run --rm tileserver"
  },
  "author": "",
  "license": "ISC"
}
